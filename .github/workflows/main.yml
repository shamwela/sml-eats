name: Cypress tests
on: [push]
jobs:
  cypress-run:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Do stuffs
        run: |
          npm install -g pnpm
          pnpm install
          pnpm build
          cd apps/client
          pnpm test
